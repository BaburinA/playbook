# Для обновления ОС серверов Drweb
---
- hosts: "DrwebUpd"
  become: true
  tasks:

  - name: Run the equivalent of "apt-get update" as a separate step
    ansible.builtin.apt:
      update_cache: yes

  - name: Upgrade apt-get dist-upgrade
    ansible.builtin.apt:
      upgrade: dist

